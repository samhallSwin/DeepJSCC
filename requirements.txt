# Core dependencies for the current DeepJSCC training and evaluation workflow.
# This file is intentionally hand-maintained and only lists top-level packages
# imported by the repo's active code paths.
#
# GPU note:
# tensorflow_compression currently requires TensorFlow 2.14.x in this project,
# so the matching OzSTAR GPU stack should use CUDA 11.8 and cuDNN 8.7 rather
# than the newer CUDA 12.6 / cuDNN 9.5 module combination.

matplotlib==3.9.2
numpy==1.26.4
pandas==2.2.3
pillow==11.0.0
scikit-image==0.24.0
sionna==0.19.0
tensorflow==2.14.1
tensorflow_compression==2.14.1
