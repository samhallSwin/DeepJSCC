# Optional extras for semantic/downstream evaluation paths.
# Install these in addition to requirements.txt if you enable CLIP or
# downstream classifier metrics in config/config.py.

-r requirements.txt

timm==1.0.11
torch==2.4.1
torchvision==0.19.1
git+https://github.com/openai/CLIP.git
